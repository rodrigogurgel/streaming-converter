import { VideoConverterHandler } from './video-converter-handler';

describe('VideoConverterHandler', () => {
  it('should be defined', () => {
    // expect(new VideoConverterHandler()).toBeDefined();
  });
});
